# Checked-product actual preparation: offline observer gate

No runtime edit or vendor execution. All14 runtime modules remain the reviewed
3306-test source (top56f341f0, checked guard7e0b6e67). This is the first concrete
enabled/disabled actual-harness source gate, not a numerical FFT result or a
launch-ready recipe. It remains independent of retained-output scheduling.

## Result and retained attempts

Original37479 terminated0: **93 PASS in5.97s**, plus ruff PASS. Persistent root:
`/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz/checked-product-actual-gate-v1.zA7kZBTI`.
All original handles are terminal and owned receipts remain intact.

- Binding v1:10PASS; disabled v1:44PASS; binding v2/v3:12/14PASS.
- Enabled v1 original75999:61PASS1.32s, both actual-harness elaborations only.
- Observer v1 original69390:14PASS2.49s.
- Observer v2 original1447:26PASS3.91s.
- Final93 combines53 source/preparation,14 binding and26 observer tests.

The deliberately failing old READY/reservation, inverse framing-only,
VALID-as-owner and READY==receipt assertions are retained raw negative controls,
not relabeled historical passes. Six corrupted independent-observation cases
terminate with their specific ACK/start/payload/release/lease/receipt fatal.
Six weakened observer source variants fail source admission even after locally
refreshing their claimed observer hash. This is distinct from executing a
weakened monitor on vendor FFT.

## Exact source closure

| Source | SHA256 |
| --- | --- |
| HDL `prepare_checked_product_actual.py` | `9d06f33d810eb69e9a9112c06756cadf3906c91935b80f53c98477c0904601e3` |
| HDL `tb/starlink_pss_checked_product_actual_binding_probe.svh` | `b0380a411f66316f73790e1654503a27a1648d457c1a177173ccc883f1cc354e` |
| HDL `tb/starlink_pss_checked_product_actual_observer.svh` | `e0fc22ae7975db53790b0fc46115dbfe9ab3bbcf18c8c37784011d7248dd5647` |
| FW `tests/starlink_oracle/test_prepare_checked_product_actual.py` | `c0ec00904856e5ec568a17d089a9b9457e60f041ffa400cbcde79777759031f2` |
| FW `tests/starlink_oracle/test_checked_product_actual_binding_probe.py` | `095cea1e50bd227949111e714b90276aa1b3cdaf765e2b20ab465bfa954ab53d` |
| FW `tests/starlink_oracle/test_checked_product_actual_observer.py` | `9206293237d4dadea52f447db89af35bab5513030617cfa012b46ad0d1144a04` |

The enabled prepared directory is the above root plus
`pytest/enabled_actual0/prepared`. All77 manifest members independently rehash
PASS; manifest `fc59a9d828598570440205ed07d5367a2a3f117731a05fc13267b839e3bed860`.
Generated bench `d9080893669c55c93108bc4175eefe23b6c3d90399b1d43cb21cf48bc85699e7`;
whole inverse receipt `c88bee4667e96c7bb886026c95e70f4a7beaa3472617c33758f65387d612cfd7`.

The old complete65-member actual inventory7adf2efa is independently pinned.
Enabled construction has40 literal edits; its entire old bench restores to
68aab935, including every removed original observer body. The verifier derives
edits again from the independently hashed original; an edited inverse JSON
cannot authorize a changed observer. All other inherited files are literal,
including vectors, factory, reference modules and old helper/runner policies.
Disabled construction restores the complete old bench and P1 binding include,
including untouched independent reference hierarchy (candidate-only aliases).
Unexpected files, source corruption, symlink aliases and overwrite are rejected.

Import closure additionally uses unchanged `test_checked_product_top.py`,
`test_checked_product_read.py`, `product_interface_graph.py`, the original
field policy and the pinned P1 actual source inventory. The frozen compiled
actor contains full old guard and shadow modules inline, not candidate aliases.
Per-build sources.json is a compiled-source snapshot, not a claim that every
Python import has been copied into every case directory.

## Executed observer scope

Both enabled and disabled actual benches elaborate using the declared control
actor, without running that actor against vendor numerical vectors. Enabled
elaboration retains two inherited dangling default-inert forward-retirement
ports in the old result shadow; disabled retains those plus the identical old
reference warnings. No runtime binding is relaxed or changed to silence them.

The new observer itself executes on the literal frozen real-controller actor
bench, with independent expected integer product, actual-input frozen full old
guard, and all original actor stimulus/assertions preserved by a whole-body
inverse. Four healthy pairs give jobs/ACKs/inverse starts/releases4/4/4/4,
2048 accepted outputs,8 capacity/receipt observations,26436 old-guard checks and
13218 equal pre/post observer samples. Healthy phase deltas remain seal+2,
publish+3 from final product; ACK+10 after publish, inverse start+9 after ACK,
first core+3 after start, last-first511, release+1. These are actor timings only.

The retained preflight case has one ACK, zero inverse starts/releases and28
continuous retained-lease checks. Checks run pre-NBA and at+2ps on both fast
edges, so a sampled take cannot disappear before the settled inspection.

All six pre-GOOD raw position37 metadata/ordinal/TLAST × core READY0/1 cases
produce exact low-four verdict4/1/2, respectively, with no delivered word37.
READY0 reaches34 consumed words before quarantine; READY1 reaches37. Offered
word evidence is checked while stalled as well as taken. This is not an
arbitrary post-GOOD transport-wire upset proof. Original source-side active
fault expressions remain literal in the generated enabled source.

Additional actor cases cover actual producer READY at37/511, head/origin fault,
current vendor/closed input events, producer malformed metadata/TLAST, raw
reader faults, and both raw reset sides. Six negative observation corruptions
leave DUT/input patterns unchanged and demonstrate independent origin/receipt,
same-token payload, final drain and retained ownership detector activity.

## Replay and remaining actual admission

From `/tmp/starlink-rom-prefetch.j829ht/fw`, use sanitized venv Python-B:

```text
python -B -m pytest -p no:cacheprovider \
  tests/starlink_oracle/test_prepare_checked_product_actual.py \
  tests/starlink_oracle/test_checked_product_actual_binding_probe.py \
  tests/starlink_oracle/test_checked_product_actual_observer.py \
  --basetemp NEW_NON_TMP_DIRECTORY --junitxml NEW_RESULTS_XML
```

Unset PYTHONHOME/PYTHONPATH/LD_LIBRARY_PATH and set PYTHONDONTWRITEBYTECODE=1.
Original raw logs/XML/builds and all14 runtime pins are preserved under the
final root; no project or vendor child was created.

The old Tcl runner is deliberately byte-identical2d3b5008 and NOT made to admit
the transformed source. The new verifier returns
`enabled_compile_only_NOT_vendor_admission`. Remaining bounded work after this
review is source-specific Tcl/settings admission and strict full actual-result
receipts, including absolute5215-fast-clock/29.8us service cap and complete
accepted-tuple/numerical/extra-epoch accounting. Declaring5215 in this offline
metadata is not yet an executable actual-result service gate. No old whole-cycle
CSV hash/raw217 pass is claimed for changed-latency enabled mode. Historical
P1 CSVs, qualified-status result and failed attempts remain immutable.
