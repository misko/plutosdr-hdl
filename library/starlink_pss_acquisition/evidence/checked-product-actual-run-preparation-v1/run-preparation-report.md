# Checked-product source-specific actual runner preparation

Offline only. Original37746 terminated0: **171 PASS34.75s** (new78 plus the
unchanged reviewed93). Prior41267:66PASS26.68s;65253:78PASS26.14s. All original
logs/XML/source snapshots are retained. No vendor process or production change.
All14 runtime modules and the reviewed93 helper/observer/binding/test bytes
remain unchanged from the published35edd0045/0e9370d5d checkpoint.

## Frozen candidate and recipe

Prepared directory:
`/home/mouse9911/gits/starlink-build-recovery-20260910.vHzUVnBz/checked-product-actual-prepared-v1`.
Its project does not exist. The84-file manifest SHA is
`9b21ff51490f0deb87ae0b69a78e65e37895bb8006f67b2615a567e70e8e63c7`.

| File | SHA256 |
| --- | --- |
| `prepare_checked_product_actual_run.py` | `893248bdb896c058b2290e9049530cc233edde5ab234f689c5a210a07a3ac16b` |
| `checked_product_actual_result.py` | `a04c648779af03ba0d156215fa274e5b2d4b92af7028fc38b95e66a35e452084` |
| `frozen_sources/simulate_exact_control_prepared.tcl` | `934766358dc477bc288fa5e97519b01394708f1b8832c61e76f19d927003acda` |
| FW `tests/starlink_oracle/test_checked_product_actual_run.py` | `1f08f038be01cf27bd390b5eed596a585c23445a6c70958be8e1731ddde26654` |

Explicit R/D/S/C/K/M/P/CHECKED/extra options are1, FAST_MHZ175 and QUICK_MUTATION0.
The source100MHz clock, frozen xFFT factory, partxc7z010clg400-1, Vivado2022.2,
two threads and all simulation directives remain literal. There are exactly
six source-specific runner replacements; the complete original2d3b5008 runner
restores byte-for-byte, including its replaced original raw217/paired-CSV tail.
No synthesis, route or clock/exception changes are present.

The new admission helper binds the full reviewed77-member offline inventory
fc59a9d8, restoring only runner/settings in memory. Old numerical stimulus,
factory, all seven vectors, actual-port guard/arithmetic/ROM/retirement/CDC
shadows and runtime remain independently hash-bound. Every11-setting omission,
duplicate and wrong value fails; altered runtime/vector/observer/factory/runner
files, unexpected actor files, overwritten outputs and symlink aliases fail.

## Result acceptance

The new candidate is changed-latency scope, not original raw217 or whole-cycle
CSV equality. The exact enabled observation adaptations were separately reviewed
and independently93-tested. The new runner calls a source-specific result
verifier instead of accepting the removed original paired/reference receipts.

- Require a unique checked ownership terminal, all inherited enabled local
  guard/control/numerical matrix terminals, exact84 preflight/12 active/12 cache
  rows and original extra2fault/3stall/2reset/4recovery counts.
- Preserve both original CDC and ROM Tcl receipt procedures literally and
  execute them independently in the result parser, including positive reset,
  private-reset ROM, current-fault and at least two final-fault witnesses.
- Preserve the original44 complete numerical blocks,24064 forward/product
  words,128..132 provisional prefix bound and exact inverse-word accounting.
  The full source-bound bench still performs each actual numerical comparison.
- Parse every row of both complete candidate CSVs, including startup four-state
  entries; require contiguous cycles and total rows equal the observer sample
  count. There is no sampled/downselected trace acceptance.
- Require32 nominal and6 repeated-stall real ACK-drain services, plus all31/5
  forward admission intervals, within **5215 fast clocks** (29.8us at175MHz).
  This is an absolute gate, not actor+8 or a newly loosened acceptance constant.
- Bind product/private publication and all core ledger tuples to the exact512
  product-vector words, full descriptor/exponent, ordinal and admitted lease;
  require publication before ACK, ACK before core, no reordered/missing tuple,
  and full ledger/terminal count agreement. Partial faulted private streams stay
  in the ledger and may not become a published numeric block.
- Reject FATAL_ERROR, kernel crash, segmentation fault, error/fatal text, actor
  scope and old paired markers even if the outer vendor process returns0.
  Require nonempty actual generated xFFT9.1 XCI and sim/synthesis VHDL, exact
  realtime512/18-bit/BFP settings, plus compilation/elaboration artifacts.

Parser positives use explicitly synthetic unit fixtures and are not vendor
results. Eight ledger negatives corrupt payload/position/lease/metadata, remove
ACK, alter counts or truncate a file. Cycle tests reject missing/reordered rows,
faulted service, missing final drain, count mismatch and5216;5215 passes.
Three mocked Tcl tests verify sanitized child Python under poisoned parent
environment, unchanged parent values, exact generic readback and failure before
real project/FFT creation or launch. No real factory or vendor tool executes.

The parent additionally ran the frozen cycle parser on both actual historical
P1 CSVs:589949+33180 rows,32/6 services,31 nominal4547/4548 intervals and5 stalled
4821..4828 intervals passed. That validates old schema/service interpretation
only; it is not candidate actual qualification or new observer-count evidence.

## Proposed one-shot invocation — not authorized or executed

Root retains vendor ownership. Before any future invocation, a root-owned
one-shot owner must independently pin this manifest/runner/helpers, resolve and
pin the Python executable, sanitize PYTHONHOME/PYTHONPATH/PYTHONOPTIMIZE and use
a new writable non-/tmp TMPDIR. Recheck absent project/log/journal/start paths,
all84 source hashes and available space. The vendor command inside that owner is:

```text
LD_LIBRARY_PATH=/opt/Xilinx/Vivado/2022.2/lib/lnx64.o/SuSE \
/opt/Xilinx/Vivado/2022.2/bin/vivado -mode batch -notrace \
  -source PREPARED/frozen_sources/simulate_exact_control_prepared.tcl \
  -log NEW_OWNER/vivado.log -journal NEW_OWNER/vivado.jou \
  -tclargs PREPARED
```

PREPARED is the explicit directory above; NEW_OWNER is a fresh root-owned path.
Log/journal must precede tclargs. Retain original process exit separately from
result acceptance. On success, failure, crash or timeout/interrupt, audit source
inventory and generated IP, preserve nonempty status receipts and all partial
files, and require exact pre/post source identity. Never restart automatically.
After the original process terminates, independently run frozen Python-B
`prepare_checked_product_actual_run.py --verify-result PREPARED` with the same
external source pins. Tool exit0 alone is never qualification.

Replay new78 with `tests/starlink_oracle/test_checked_product_actual_run.py`;
full171 adds the three93-gate test files documented in the preceding report.
Use sanitized Python-B, no pytest cache, fresh persistent basetemp/log/XML.
This report requests source review only; no actual/synthesis/route/RF authority.
